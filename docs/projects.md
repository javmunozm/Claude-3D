# Projects

<!-- Add one line per project as they are created -->
| Folder | Description | Status |
|--------|-------------|--------|
| BedLifter | 30 cm head-end bed raiser (8.85° incline): HeadLifter + MiddleLifter adapters, 3D print PETG | Macro ready |
| BrokeFeet | Left clubfoot skeleton from a patient CT study, for pre-operative teaching. Two generations nested as `v0/` (original pipeline, still the upstream dependency) and `v2/` (crater-corrected, hand-sculpted, joint-pinned) | **v2 print-ready** — slice `v2/BrokeFeetV2_pinned2.stl`; **not** for surgical planning |
| NespressoPodDispenser | Forward-facing gravity Nespresso pod cage; solid-wall rebuild of a hex-perforated reference, for faster printing | Print 1 failed (filament ground); base-repair foot ready |
| VitaGrip1000 | Grip for the PS Vita **PCH-1000**: a slim cross-bar low across the console's **rear** with two **DualSense-contoured** handles at its ends. Console front face, rear touchpad and shoulder buttons all stay exposed. Two generations in one folder — `v0/` solid, `v2/` hollow shell with palm swell, finger grooves, grip texture and internal ribs. | **v2 ships, not yet test-printed.** 223.30 x 106.08 x 45.57 mm, **77.7 cm3** (3 mm shell; v0 was 149.0 solid). Slot ray-probed to 19.320 mm, 0/200,000 console points interfere, 0 mm3 sealed void, wall 3.02 mm median, 3/3 ribs at 1.96 mm. The blueprint set's 173 mm width is impossible for a 182 mm console and is used for FEATURES ONLY. Build `v2\macros\vita_grip.py`, verify with `fit_check.py` + `feature_audit.py`. See README |
| VitaGripPS5 | PS5-style grip for the PS Vita **PCH-1000**, handles contoured after a DualSense. Built from scratch; shares nothing with VitaGrip1000. | **RESTARTING — no geometry.** The swept-tube figure was rejected 2026-09-08 ("doesn't match anything") and its STL + build macros deleted; it passed every dimensional gate (profile within 3.7 mm, oval section) while still reading as two lumps on a stick, because its shape came from a 3/4 CAD render misread as a side view. Kept: the watertight **DualSense scan** in `derived/` (tip span 120.442 mm, matches the user's caliper), `console_solid.stl`, `prepare_dualsense.py`, `make_console_cutter.py`, `fit_check.py`. Awaiting a DualSense STL from the user. See README |
| VitaGrip-hori | Adapt the real **Hori Remote Play Assist** grip mesh (`sources/psvitaGrip/grip2000/`, built for the PCH-2000 slim) so a **PCH-1000** fits — split-and-widen, +3.6 mm depth only. | **Adapted, not yet test-printed.** Both shells widened: rear 32.94→36.54 mm, front 36.91→40.51 mm; pocket wrap 11.50→15.25 (rear) and 7.75→11.25 mm (front). Both watertight, 1 body, 0 bad edges. Hori geometry below the cut preserved to 0.000 mm². Regenerate with `macros/widen_hori_grip.py` (STLs gitignored, 110–134 MB). See README |
